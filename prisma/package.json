{
  "name": "quizlearn-prisma",
  "version": "1.0.0",
  "description": "Prisma schema and migrations for QuizLearn",
  "scripts": {
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "generate": "prisma generate",
    "seed": "ts-node seed.ts"
  },
  "devDependencies": {
    "@prisma/client": "5.22.0",
    "@types/node": "^20.10.4",
    "prisma": "5.22.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
